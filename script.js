let humanSpeed = 5;
let sharkSpeed = 10;

let humanDistance = +prompt("humanDistance");
let sharkDistance = +prompt("sharkDistance");

humanDistance / humanSpeed < sharkDistance / sharkSpeed
  ? alert("Akula yetolmaydi")
  : alert("Akula yetib oladi");

// Akula algorithm
