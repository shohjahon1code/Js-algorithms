// Kiritgan sonignizni ket-ketlikda qo'shib berish algorithmi

let num = parseInt(prompt("Enter the number!"));

let answer = 0;

for (let i = 0; i <= num; i++) {
  answer = answer + i;
}

console.log(answer);
